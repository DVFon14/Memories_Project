export const CREATE = "CREATE";
export const UPDATE = "UPDATE";
export const DELETE = "DELETE";
export const FETCH_ALL = "FETCH_ALL";

//Now, if we misspell something in client > src > actions > post.js, catching the error
//will be significantly easier than if we had kept them as strings
